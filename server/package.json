{
  "name": "daebit-server",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "dotnet run --launch-profile \"Production\"",
    "start:dev": "dotnet watch run --launch-profile \"Development\" --urls \"http://*:5000\"",
    "publish": "git add . && git commit -m \"Publish from cli\" && git push"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {}
}
